# Agent notes

Work in `titan-journal/` unless the user explicitly asks to change TITAN COT (`titan-dashboard/`, `cot-data-module/`).

Keep it simple. Do not add features, files, or process the user did not ask for.

## Cursor Cloud specific instructions

Do not record videos, take walkthrough screenshots, or produce demo recordings unless the user explicitly asks for them.

Do not run browser / GUI walkthroughs unless the user explicitly asks. Prefer existing unit tests (`titan-journal`: `npm test`). Skip inventing extra test infrastructure.

Do not subscribe to GitHub CI, PR watches, or similar notifications unless the user explicitly asks.

Do not merge a pull request unless the user says `mergnout` / `mergni` / `Mergni to`. `Super` is approval of the work, not a merge. `pushni to` means push.

Reply to status in Czech and keep it short. Do not explain agent internals, test plans, or artifact pipelines.
